// Single source of truth for the BRAND name (the company/app name shown to users).
// Change these two lines to rebrand everywhere at once.
// NOTE: the word "pod" used for a *group of people* is product vocabulary and is
// intentionally separate from this — it stays "pod" regardless of the brand name.
export const BRAND_NAME = "Pod";
export const BRAND_MARK = "🫛";
